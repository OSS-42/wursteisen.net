# Site content (edit these, then push `main`)

- `work.json` — jobs on the Experiences panel. Add a PNG in `src/assets/workXP/` whose filename matches a `"logos"` entry (`alithya.png` → `"alithya"`).
- `site.json` — LinkedIn, CV file, SEO strings.
